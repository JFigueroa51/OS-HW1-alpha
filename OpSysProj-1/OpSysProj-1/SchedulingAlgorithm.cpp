#pragma once
#include "stdafx.h"
#include "SchedulingAlgorithm.h"

Request SchedulingAlgorithm::nextRequest()
{
	//TODO: throw error if this function is ever called
	return  Request();//had to return request for compiler, but this should never be called
}
